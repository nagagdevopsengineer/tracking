apiVersion: v1
kind: Service
metadata:
  name: tracking-dev
  namespace: tracking-location
  labels:
    app: tracking-dev
spec:
  type: NodePort
  ports:
  - name: https
    port: 80
    targetPort: 3000
  selector:
    app: tracking-dev