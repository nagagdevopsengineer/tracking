apiVersion: v1
kind: Service
metadata:
  name: tracking
  namespace: tracking
  labels:
    app: tracking
spec:
  type: NodePort
  ports:
  - name: https
    port: 80
    targetPort: 3000
  selector:
    app: tracking